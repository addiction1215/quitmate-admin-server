spring:
  datasource:
    url: jdbc:h2:mem:~/quitmate;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
    properties:
      hibernate:
        default_batch_fetch_size: 100
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          exception-handling: ignore
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration
      - org.springframework.boot.autoconfigure.data.mongo.MongoDataAutoConfiguration
  mail:
    host: smtp.gmail.com
    port: 587
    username: quitmate1215@gmail.com
    password: test
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true

jwt:
  secret-key: 4a0pPV9g4/6OvKKzV1MpVdIchBeTfcjd/TRKbuWUgpNY3jyh6YA/aBnpe4kPsAY5WJoBBzOG358zqS5/E0TSag==

aws:
  access-key: testaccesskey  # AWS IAM Access Key
  secret-key: testsecretkey  # AWS IAM Secret Key
  s3:
    region: ap-northeast-2  # Seoul Region
    bucket:
      user: testuserbucket  # 사용자 관련 파일 버킷
      challenge: testchallengebucket  # 챌린지 관련 파일 버킷
      challenge-badge: testchallengebadgebucket  # 챌린지 배지 관련 파일 버킷